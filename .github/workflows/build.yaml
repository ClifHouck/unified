name: Build

on:
  workflow_call:

permissions: {}

jobs:
  build:
    name: Build Unified 
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - uses: actions/checkout@v4
      - name: Build
        uses: ./.github/actions/build/
